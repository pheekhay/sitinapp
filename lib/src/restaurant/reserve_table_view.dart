import 'package:flutter/material.dart';

class TableBookings extends StatelessWidget {
  const TableBookings({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
